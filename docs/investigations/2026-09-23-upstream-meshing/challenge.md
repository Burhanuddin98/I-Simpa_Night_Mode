All five checked load-bearing claims HOLD with primary evidence re-derived independently (not just re-read of prior artifacts): `tetgen.cxx:27347-27388` (check_subface, radius/emin only, no area read — I opened it myself), the AGPL-3/WIAS dual license text in `src/tetgen/LICENSE`, the area-patch-insufficient result reproduced in `arms.json` (`area_q2_var`/`area_q5Y_var`: still 6 tets/2 floor faces with the 10-line patch applied), the merged-quad-facet result in `arms_merged.json` (patched 2130 tets/942 faces/max 0.1117 m² vs pinned-exe 9 tets/3 faces/max 30 m²), and the MSVC right-to-left argument-evaluation claim — I reran `t_O2.exe`/`t_Od.exe` myself and got `a=43 b=32 c=21 d=10` at both `/O2` and `/Od`, matching the synthesis exactly.

One gap the synthesis doesn't flag as loudly as it should: the evalorder test (`evalorder/t.cpp`) is a synthetic analog — an `ivec4` constructor with four `ToInt(GetNextToken())-1` arguments — not a compiled probe of the actual `Objet3D_maillage.cpp:160-163` call site. It's evidence that MSVC 19.44's right-to-left convention is real and reproducible, not evidence that the real GUI code path has the same argument shape/side-effect ordering (e.g. if the real call has fewer or reordered side-effecting sub-expressions, the same compiler convention could still yield a different result). The synthesis itself marks the "shipped GUI actually writes that order" step as [inferred] and separately flags U1 to settle it by running `isimpa.exe` — that caveat is correctly placed, so this isn't a hole, just worth underlining: U1 is not optional polish, it's the only thing standing between "most likely does not crash" and an unverified guess, given the crash margin is 2.4e-7 in float32.

No prior FEEDBACK.md or memory-dir entries exist for this design shape (TetGen version pinning / area-constraint regression) — searched both, no hits, so this is not a repeat failure being reviewed blind.

VERDICT: survives
No file paths beyond those already cited above needed further correction; all five re-checked claims held under independent verification (`B:/repos/I-Simpa-upstream/src/tetgen/tetgen.cxx:27347`, `B:/repos/I-Simpa-upstream/src/tetgen/LICENSE`, `B:/repos/I-Simpa_Night_Mode/target/investigate/synthesis/arms.json`, `.../arms_merged.json`, `.../evalorder/t_O2.exe` + `t_Od.exe` rerun).