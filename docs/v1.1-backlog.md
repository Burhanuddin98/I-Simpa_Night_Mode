# v1.1 backlog

Work that was **deliberately deferred** from v1, not forgotten. It was deferred under Burhan's filter
(2026-09-25, 23:53): something goes into v1 only if it **stops a wrong number reaching a user**, or it
is **a feature Burhan asked for**. Everything else comes here.

How to use this file:
- **Nothing deferred lives anywhere else.** A deferral that exists only in a chat, a handoff or an
  agent's report does not count. Add it here, with its receipt, in the same session.
- **Every item has a "done when".** It is closed only when that test passes, with the commit named.
  Burhan alone can drop an item, and a dropped item stays listed, struck through, with his words.
- **Gate:** milestone M14 (`docs/rebuild-plan.md`) is this list, closed or dropped. M13's release
  notes link here, so users can see what is coming.

| # | Item | Why it waits | Origin (receipt) | Done when | Status |
|---|---|---|---|---|---|
| 1 | **An exact sphere reference for M8.** A Lambert sphere's decay rate from the renewal equation `(1−α)·E[e^(δl/c)] = 1` with the sphere's chord distribution: theory, not our code. | M8's boxes are already covered by Kuttruff at 5 % and the transport (gated tight). This guards against a misconception shared by the transport and SPPS. | Chat 2026-09-25 23:51; `session-logs/HANDOFF-2026-09-23.md` | SPPS on faceted spheres at two or more mesh refinements, extrapolated to the ideal sphere, agrees with the exact rate within its Monte-Carlo noise. A say-NO: a wrong α per reflection fails it. | open |
| 2 | **A real-room bed against the BRAS measurements** (S09-S11 = CR2-CR4). | This tests whether the model fits reality, not whether the solver is right. It is reported, not gated. | HANDOFF, M8 decisions of 2026-09-24 17:45 (decision 1, "Later") | T30, EDT, C80 and D50 are simulated against the measured RIRs with the absorption uncertainty stated, and the report is committed. | open |
| 3 | **Make M6 (a) and (c) able to fail.** Today they compare byte-identical meshes. | Every M6 check passes, and the comparators' say-NOs prove they can discriminate. | pre-M8 sentinel, 2026-09-25 23:12 (`gate_m6.txt`, mbin sha256 EC8AA0389F989890) | Each check compares non-identical inputs, or is renamed to what it really checks. A planted mesh change fails it. | open |
| 4 | **Point the gates' deliberate say-NO runs at their own work folder.** | They leave 13 small entries in `target/tmp` and 18 in parity-bed per full gate run, by design. It is housekeeping. | pre-M8 gates report, 2026-09-25 23:01 | A full gate run leaves 0 new entries in `target/tmp` and parity-bed. | open |
| 5 | **Close the receivers' 0.6 % on the receivers themselves** (one-sided 95 % bound 0.607 % in the worst cell). | It holds today through the room-energy argument (paired difference within 1.4 SE). About 16 times the rays in that one cell would close it directly. | pre-M8 critic, 2026-09-25 23:12; piece A report | The receivers' own bound is ≤ 0.6 % in every M8 cell. | open |

## v2 contenders

Burhan's rule for this list (2026-09-26 02:33, verbatim): **"WE LEAGE THIS AS A SERIOUS V2 CONTENDER, ONCE V 1.1 IS DONE AND READY"**.
Nothing here starts before M14 (this backlog's v1.1 items) is closed.

| # | Item | Why | Origin | Done when | Status |
|---|---|---|---|---|---|
| V2-1 | **Our own GPU particle tracer** (CUDA, Grace's RTX 5070), alongside upstream's CPU SPPS. | The EDT band shows that tight early parameters need fine steps (about 0.1-0.5 ms), and noise needs many particles (random-mode T30 about 20-35 M, inferred). Both are pure compute. SPPS is CPU-only. It is a new solver, so it breaks v1's rule of "upstream's solvers, unchanged" and needs its own full validation. | Chat 2026-09-26 02:32 ("what if we could use the GPU"); the EDT-band design's open issues (`target/agents/edt-band/`) | It passes the same M8 bed as SPPS (the Kuttruff reference, the independent transport, the error bands). It agrees with SPPS within noise on the parity rooms. Its speed-up is measured on Grace against SPPS at equal step and particle count. | open, after M14 |
